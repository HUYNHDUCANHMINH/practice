        let a = +prompt("nhap so a: ");
        let b = +prompt("nhap so b: ");
        if (a % b == 0) {
            alert("So " + a + " la boi so cua " + b)
        } else {
            alert("So " + a + " khong la boi so cua " + b)
        }
        if (a % b == 0) {
            document.write("So " + a + " la boi so cua " + b)
        } else {
            document.write("So " + a + " khong la boi so cua " + b)
        }